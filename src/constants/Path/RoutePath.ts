export enum RoutePath {
  Splash = "/splash",
  Home = "/home",
  Auth = "/auth",
  Main = "/main",
  Chat = "/chat",
  Community = "/community",
  Diary = "/diary",
  DiaryDetail = "/diary/write",
  More = "/more",
}
